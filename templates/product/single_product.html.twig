
    <div class="product-item text-center">
        <a href="{{ path('app_product', {'slug': product.name}) }}"><img src="uploads/Illustration/photos/{{ product.illustration  }}" alt="{{ product.name }}" class="img-fluid"></a>
        <h5>{{ product.name }}</h5>
        <span class="product-subtitle">{{ product.subtitle }}</span>
        <span class="product-price">{{ product.price /100}}€</span>
    </div>
