{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}La Boutique Française{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-4">
         <img src="/uploads/Illustration/photos/{{ product.illustration  }}" alt="{{ product.name }}" class="img-fluid">
        </div>
        <div class="col-md-8">
        <h3>{{ product.name }}</h3>
        <p>{{ product.subtitle }}</p>
        <hr>
       <span class="product-page-price"> {{  product.price /100}} €</span>
        <hr>
        <p>
            {{ product.description }}
        </p>
            <a href="{{ path('app_carte' , {'id': product.id}) }}" class="btn btn-primary">Ajouter au panier</a>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
            <br>
        </div>




    </div>

{% endblock %}




